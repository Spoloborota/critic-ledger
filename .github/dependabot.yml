# The workflow pins every action to a full commit SHA, so upgrades cannot
# arrive silently through a moving tag. Dependabot is what makes those pins
# maintainable: it opens a pull request when a pinned action has a new release,
# with the SHA already resolved.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
